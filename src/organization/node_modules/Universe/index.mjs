import registry from 'registry';

import Map from 'Map';

export default class Universe {

  map = new Map();

  constructor(){
  }

  async initialize(){
    await this.map.initialize();
    registry.universe = this;
  }

  location(id){
    this.map.location(id);
  }

}
