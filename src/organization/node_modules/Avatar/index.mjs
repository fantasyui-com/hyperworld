import registry from 'registry';

export default class Avatar {

  #location = null;

  constructor(){
  }

  async initialize(){
    this.#location = registry.universe.location('lobby');
  }

  get location(){
    return this.#location;
  }
  set location(id){
    this.#location = registry.universe.location(id);
  }

}
