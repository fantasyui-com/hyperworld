import registry from 'registry';

import User from 'User';

export default class Session {

  user = new User();

  constructor(){
  }

  async initialize(){
    if(!registry.universe) throw new Error('')
    await this.user.initialize();

    registry.session = this;
  }

}
